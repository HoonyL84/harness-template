# Gemini Entry Point

This repository uses `AGENTS.md` as the canonical agent instruction file.

Read `AGENTS.md` first, then follow the entry checklist it defines. Project context lives in `docs/project/PLANS.md`, and reusable agent skills live under `skills/<skill_name>/SKILL.md`.
