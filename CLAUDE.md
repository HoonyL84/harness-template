# Claude Entry Point

This repository uses `AGENTS.md` as the canonical agent instruction file.

Read `AGENTS.md` first, then follow the entry checklist it defines. Portable skills live under `skills/<skill_name>/SKILL.md` and should include YAML frontmatter with at least `name` and `description`.
